package com.ninja.scan.ui

import androidx.compose.ui.graphics.Color

/**
 * Shared semantic icon colors, used consistently for the same action
 * everywhere it appears across Documents, Cards, the viewer, and the page
 * editor — so e.g. delete always reads as red, share always as blue.
 */
val DestructiveRed = Color(0xFFEA4335)
val ActionGreen = Color(0xFF34A853)
val ShareBlue = Color(0xFF4285F4)
val EditAmber = Color(0xFFFBBC05)
val WhatsAppGreen = Color(0xFF25D366)
